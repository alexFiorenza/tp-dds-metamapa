apiVersion: 1

datasources:
  - name: MetaMapa PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: metamapa_db
    user: metamapa
    secureJsonData:
      password: metamapa123
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    isDefault: true
    editable: true
